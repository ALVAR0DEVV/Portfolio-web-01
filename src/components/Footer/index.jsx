import styles from './Footer.module.css';

function Footer() {
    return (
        <footer className={styles.footer}>
           Atividade simples com react+vite+routes
        </footer>
    )
}

export default Footer;